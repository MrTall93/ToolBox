apiVersion: v1
kind: Service
metadata:
  name: toolbox-mcp-debug
  namespace: toolbox
  labels:
    app: toolbox-mcp
    app.kubernetes.io/name: toolbox
    app.kubernetes.io/component: mcp-server
spec:
  selector:
    app: toolbox-mcp
  ports:
  - name: debug
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP